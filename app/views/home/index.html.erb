<% @posts.each do |post| %>
<div class="col-md-2">
	<img src="<%= post.image %>" alt="" class="img-responsive">
	<div class="row"><p class="text-center"><%= post.description %></p></div>
</div>
<% end %>